{
  "name": "@chatbot/widget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/widget.ts --bundle --minify --outfile=dist/widget.js --platform=browser --target=es2018",
    "dev": "esbuild src/widget.ts --bundle --outfile=dist/widget.js --platform=browser --target=es2018 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.0.0"
  }
}
